# Minha Evolução v4

Antes de publicar, execute `migracao_v4.sql` no SQL Editor do Supabase.
Depois substitua no GitHub `hevy_dashboard.py` e `requirements.txt`.

Mantém os mesmos Secrets: APP_PASSWORD, HEVY_API_KEY, GROQ_API_KEY, SUPABASE_URL e SUPABASE_KEY.

Principais mudanças: layout mobile-first; aba Dados removida; configurações na engrenagem; medidas em português; plano alimentar calculado pela IA; rotina semanal por modalidade/frequência/duração/intensidade; cadastro manual de treino; IA preservada.
